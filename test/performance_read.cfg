process PREAD = {
  include "FWCore/MessageLogger/data/MessageLogger.cfi"
    
    source = EmptySource {
	untracked int32 maxEvents = 1
	untracked uint32 firstRun = 1   
	untracked uint32 numberEventsInRun = 1
    }
    
    # service = Timing {}
    
    es_source = PoolDBESSource { 
        VPSet toGet = {{ string record = "SiStripPerformanceSummaryRcd"    string tag = "mytest_p" }}
	string connect = "sqlite_file:psummary.db"    
	#
	string timetype = "runnumber" 
	PSet DBParameters = {
	    untracked string authenticationPath="/afs/cern.ch/cms/DB/conddb"
	    untracked int32 messageLevel=2
#blob	    untracked string BlobStreamerName="DefaultBlobStreamingService"
	}
    }
    
      module prod =  SiStripPerformanceSummaryReader { }

      path p = { prod }
      module print = AsciiOutputModule {}
      endpath ep = { print }
}
